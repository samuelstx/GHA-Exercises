# This is a basic workflow to help you get started with Actions

name: Hello

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "Exercise-3" branch
  push:
    branches: [ "Exercise-3" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "test"
  generar-codigo:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      
      - uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "temurin"

      # Runs a single command using the runners shell
      - name: Run a java command 
        run: java -jar openapi-generator-cli.jar generate -i Estadisticas.yaml -g python-flask -o ./openapi_server

      - name: Run a zip command
        run: zip -r openapi_server.zip openapi_server
      
      - uses: actions/upload-artifact@v4
        with:
          name: Upload Artifact
          path: ./openapi_server
